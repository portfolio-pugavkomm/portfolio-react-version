import { LandingActiveSkillsList } from '@features/landing-active-skills-list';

export const BioPage = () => {
  return (
    <div>
      <p>Bio page</p>
      <LandingActiveSkillsList />
    </div>
  );
};
