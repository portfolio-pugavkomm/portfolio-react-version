name: "CI develop"

on:
  push:
    branches:
      - master
      - dev
  pull_request:
    branches:
      - master
      - dev

jobs:
  lint:
    uses: ./github/workflows/ci_lint_runner.yml

  test:
    uses: ./github/workflows/ci_test_runner.yml
